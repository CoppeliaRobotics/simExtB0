local simB0={}

--@fun example
--@arg int ui the ui handle
--@arg int widget the widget identifier
--@arg int index the index (0-based) where the new row will appear
function simB0.example(ui,widget,index)
end

return simB0
